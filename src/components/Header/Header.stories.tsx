import Header from './Header';

export default {
  component: Header,
  title: 'Header',
  tags: ['autodocs']
};

export const Default = {
  parameters: {
    nextjs: {
      appDirectory: true
    }
  }
};
