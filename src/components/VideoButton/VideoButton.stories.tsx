import VideoButton from './VideoButton';

export default {
  component: VideoButton,
  title: 'Video button',
  tags: ['autodocs']
};

export const Default = {
  parameters: {
    nextjs: {
      appDirectory: true
    }
  }
};
